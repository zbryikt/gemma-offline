# Podfile for gemma iOS app
platform :ios, '15.0'

target 'gemma' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  use_modular_headers!

  # MediaPipe dependencies for Gemma LLM
  pod 'MediaPipeTasksGenAI'
  pod 'MediaPipeTasksGenAIC'
end